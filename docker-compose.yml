services:
  server:
    build: .
    image: arus:local
    container_name: arus-server
    restart: unless-stopped
    ports:
      - "8787:8787"
    env_file: .env
    volumes:
      - arus-data:/app/data
    command: ["python", "-m", "app.server"]

volumes:
  arus-data:
    name: arus-data
